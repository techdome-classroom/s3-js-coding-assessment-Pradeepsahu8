/**
 * @param {string} s
 * @return {boolean}
 */
var isValid = function(s) {
    
const bracketMap={
    ')':'(',
    '}':'{',
    ']':'[',

};

const stack =[];
if(bracketMap[char])
{
    const topElement = 
}

};

module.exports = { isValid };


